import { createMuiTheme } from '@material-ui/core/styles'

export default createMuiTheme({
  palette: {
    primary: {
      main: '#000000'
    },
    secondary: {
      main: '#003d72'
    },
    error: {
      main: '#c1c1c1'
    },
    textPrimary: {
      main: '#229922'
    }
  }
})
